
// 1. Дан массив [4,7,1,3,9,3,6,2,6,9]
// вывести в консоль КОЛИЧЕСТВО (штук) четных чисел из этого массива
// 2. найти наименьшее число из этого же массива

// let arr = [4,7,1,3,9,3,6,2,6,9];
// let count = 0; 
// for (let a = 0; a < arr.length; a++){
//     if (arr[a] % 2 === 0){
//         count++
//     }

// }
//     console.log (count);

    // Объявление массива

    // let arr = [4,7,1,3,9,3,6,2,6,9];
    // Создаётся массив arr из 10 чисел.
    
    // Инициализация счётчика
    
    // let count = 0;
    // Переменная count будет хранить количество элементов, удовлетворя­ющих условию. Изначально — 0.
    
    // Начало цикла
    
    // for (let a = 0; a < arr.length; a++) {
    // let a = 0 — счётчик a устанавливается в 0 (первая позиция в массиве).
    
    // a < arr.length — проверяем, что a меньше длины массива (10).
    
    // a++ — после каждой итерации увеличиваем a на 1.
    
    // Доступ к элементу массива
    // Внутри цикла при каждом проходе текущий элемент массива — arr[a].
    
    // Условие if (arr[a] % 2)
    
    // Выражение arr[a] % 2 даёт остаток от деления на 2:
    
    // Для чётных чисел остаток = 0 → считается false.
    
    // Для нечётных чисел остаток = 1 → считается true.
    
    // Таким образом, блок if выполняется только для нечётных чисел.

    
    // Увеличение счётчика
    
    // if (arr[a] % 2) {
    //   count++;
    // }
    // Если текущий элемент нечётный, к count прибавляется 1.
    
    // Результат после цикла
    // После завершения всех итераций в count будет количество нечётных чисел в массиве. Если нужно было считать чётные, условие надо было бы записать так:
    
    
    // if (arr[a] % 2 === 0) {
    //   count++;
    // }
    // Вывод
    // Чтобы увидеть результат, после цикла можно добавить:
    
    // console.log(count);
    // и получить в консоли число нечётных (или чётных при корректном условии).

// 2. найти наименьшее число из этого же массива

const array = [4,7,1,3,9,3,6,2,6,9];
let min = array[0];
for (let i = 1; i < array.length; i++){
if (array[i] < min){
    min = array[i];
}
}
console.log(min);